@model TallinnaRakenduslikColllegeTARpe24_ChristoferKrabbi.Models.Student

@{
	ViewData["Title"] = "Kustuta õpilane";
}

@if (Model.Enrollments !=null)
{
	foreach (var courseEnrollment in Model.Enrollments)
	{
		<dl class="row">
			<dd class="col-sm-10">@(courseEnrollment)</dd>
		</dl>
	}
}
@if(Model.DeepBackstory != null)
{
	@Model.DeepBackstory;
}
@if (Model.Images != null)
{
	foreach (var courseEnrollment in Model.Images)
	{
		
		<dl class="row">
			<dd class="col-sm-10">PlaceholderText</dd>
		</dl>
	}
}